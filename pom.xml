<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.cezicola.cardplatform</groupId>
    <artifactId>card-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>card-service</module>
    </modules>

    <properties>
        <compiler-plugin.version>3.14.1</compiler-plugin.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.version>3.33.3</quarkus.platform.version>
        <opentelemetry.version>1.62.0</opentelemetry.version>
        <surefire-plugin.version>3.5.4</surefire-plugin.version>
        <jacoco-plugin.version>0.8.13</jacoco-plugin.version>
        <!-- Benchmarks are excluded from the normal build; set to "none" to include them. -->
        <test.excludedGroups>benchmark</test.excludedGroups>
        <!--
            Coverage floors, set from what the suite actually reaches rather
            than from what would sound good: 88.68% of instructions and 70.04%
            of branches across the bundle, and 93.23% of the domain package,
            measured after the credit path was proven under concurrency on
            PostgreSQL. Each floor sits
            just under the measurement, so the gate catches erosion on the next
            commit instead of failing on this one.

            Raised when the suite earns it. Never lowered to make a build pass —
            a floor that moves down to meet the code is not a floor.
        -->
        <coverage.instruction.minimum>0.87</coverage.instruction.minimum>
        <coverage.branch.minimum>0.68</coverage.branch.minimum>
        <coverage.domain.minimum>0.91</coverage.domain.minimum>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                GHSA-rcgg-9c38-7xpx, and the reason this is a BOM rather than the
                single pinned artifact it started as.

                Raising opentelemetry-api on its own left the API at one version
                and the SDK and context at the platform's, which compiles and
                then fails at startup on a class the newer API expects the older
                core to provide. A tracing stack is one artifact set or it is
                broken; the BOM is what says so. Declared before the Quarkus
                platform because the first import in dependencyManagement wins.
            -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>${opentelemetry.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
                Transitive dependencies pinned above the versions the platform
                brings, because the dependency scan found known vulnerabilities
                in them. None is declared by this project: they arrive through
                Kafka, Vert.x and the metrics stack, which is exactly why the
                scan reads the resolved tree instead of the declared one.

                Each is a patch or minor bump within the same API, and the whole
                suite is the evidence that nothing moved underneath.
            -->
            <dependency>
                <!-- GHSA-8c42-7qj2-3j46 -->
                <groupId>io.netty</groupId>
                <artifactId>netty-codec-http</artifactId>
                <version>4.1.137.Final</version>
            </dependency>
            <dependency>
                <!-- GHSA-xx22-p4ch-683r, reached through the Kafka client -->
                <groupId>at.yawk.lz4</groupId>
                <artifactId>lz4-java</artifactId>
                <version>1.11.1</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler-plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire-plugin.version}</version>
                    <configuration>
                        <systemPropertyVariables>
                            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
